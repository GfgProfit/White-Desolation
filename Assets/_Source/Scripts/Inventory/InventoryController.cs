using System.Collections.Generic;
using UnityEngine;

public class InventoryController : MonoBehaviour
{
    //[SerializeField] private RectTransform _content;
    //[SerializeField] private InventoryCell _cellPrefab;

    //public List<InventorySlot> Items { get; private set; } = new List<InventorySlot>();

    //public void AddItem(InventorySlot inventorySlot)
    //{
    //    Items.Add(inventorySlot);

    //    InventoryCell cell = Instantiate(_cellPrefab, _content);
    //    _cellPrefab.SetData(inventorySlot);
    //}
}